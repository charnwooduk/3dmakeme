# Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Environment: development or production
ENVIRONMENT=development

# Development Database
DEV_DB_NAME=wordpress_dev
DEV_DB_USER=wordpress_dev
DEV_DB_PASSWORD=change_this_dev_password
DEV_DB_ROOT_PASSWORD=change_this_dev_root_password
DEV_DB_PORT=3307

# Production Database
PROD_DB_NAME=wordpress_prod
PROD_DB_USER=wordpress_prod
PROD_DB_PASSWORD=change_this_prod_password
PROD_DB_ROOT_PASSWORD=change_this_prod_root_password
PROD_DB_PORT=3308

# WordPress Development
DEV_WP_PORT=8090
DEV_PMA_PORT=8091
DEV_MAILHOG_SMTP=1026
DEV_MAILHOG_WEB=8026
DEV_WP_DEBUG=true
DEV_WP_TABLE_PREFIX=lmf_

# WordPress Production
PROD_WP_PORT=8092
PROD_PMA_PORT=8093
PROD_WP_DEBUG=false
PROD_WP_TABLE_PREFIX=lmf_

# Site URL (update for production)
DEV_SITE_URL=http://localhost:8090
PROD_SITE_URL=https://3dmake.me

# Email Configuration
SMTP_HOST=mailhog_dev
SMTP_PORT=1025
SMTP_SECURE=false

# Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_PATH=./backups
